<div class="columns-wrap">
    <div class="nav">
        <ul>
            <li class="selected">
                Posts
            </li>
            <li>
                <a href="/collections">Collections</a>
            </li>            
        </ul>
    </div>
    <div class="content-area">    
    {{#if editorsPicks}}
        <ul class="cards wide">
        {{#each editorsPicks}}
            <li>
            {{{html}}}
            </li>
        {{/each}}
        </ul>
    {{/if}}
    {{#if featured}}
        <ul class="cards">
        {{#each featured}}
            <li>
            {{{html}}}
            </li>
        {{/each}}
        </ul>
    {{/if}}
    </div>
</div>

