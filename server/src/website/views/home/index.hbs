<div class="columns-wrap">
    <div class="nav">
        <ul>
            <li class="selected">
                Recommended
            </li>
            <li>
                <a href="/records/new">New Records</a>
            </li>            
            <li>
                <a href="/collections">Collections</a>
            </li>            
        </ul>
    </div>
    <div class="content-area">    
    {{#if editorsPicks}}
        <ul class="cards">
        {{#each editorsPicks}}
            {{> recordcard summary}}
        {{/each}}
        </ul>
    {{/if}}
    {{#if featured}}
        <ul class="cards">
        {{#each featured}}
            {{> recordcard summary}}
        {{/each}}
        </ul>
    {{/if}}
    </div>
</div>

